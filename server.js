var express = require('express');var initRoutes = require('./routes/main');var initMiddleware = require('./config/middleware');var initDatabase = require('./config/database');var app = express();initDatabase();initMiddleware(app);initRoutes(app);app.listen('3000', () => {    console.log('Server is up and running on port numner ');});app.use(function(req, res, next) {    res.render('error');});module.exports = app;